using UnityEngine;

public class HealthChange : MonoBehaviour
{
    /*
    public bool healPlayer = false;
    public bool damagePlayer = false;
    public bool overTime = false;
    public int damageToPlayer = 10;
    public int healingToPlayer = 10;
    public float timeDuration = 1.0f;
    public HealthBar playerHealthBar;

    if(healPlayer == true)
    {
        if(overTime == true)
        {
            playerHealthBar.HealOverTime(healingToPlayer, timeDuration);
        }
        else
        {
            playerHealthBar.Heal(healingToPlayer);
        }
    }

    if(damagePlayer == true)
    {
        if(overTime == true)
        {
            playerHealthBar.DamageOverTime(damageToPlayer, timeDuration);
        }
        else
        {
            playerHealthBar.Damage(damageToPlayer);
        }
    }
    */
}
